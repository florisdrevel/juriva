[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app"
```
5. Click **"Commit changes"**

---

## ALSO UPDATE `runtime.txt`

1. Click on `runtime.txt` in your repo
2. Click the pencil icon to edit
3. Change it to:
```
python-3.11.0
